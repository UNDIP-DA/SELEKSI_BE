query PembukaanJalurGetList($take: Int) {
  pembukaanJalurGetList(take: $take) {
    data {
      id
      nama_custom
      Jalur {
        id
        nama_jalur
        kode_jalur
      }
    }
  }
}
