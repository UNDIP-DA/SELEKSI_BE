query GetListPendaftaran($statusPenerimaan: Int, $pembukaanJalurId: Int) {
  getListPendaftaran(
    status_penerimaan: $statusPenerimaan
    pembukaan_jalur_id: $pembukaanJalurId
  ) {
    total
  }
}
